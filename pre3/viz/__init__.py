"""
Graphviz DOT rendering for DPDAs and NPDAs.

Produces DOT source you can render with `dot -Tpng < dpda.dot > dpda.png`.
No graphviz binary is required to *generate* the source; only to render it.
"""
